import React from 'react'
import Playground from '../../components/Playground'
import JsonRenderString from './JsonRenderString'
import JsonRenderStringRaw from './JsonRenderString.tsx?raw'
import JsonRenderObject from './JsonRenderObject'
import JsonRenderObjectRaw from './JsonRenderObject.tsx?raw'

# JsonRender

JsonRender 组件用于展示 JSON 字符串的渲染结果。只是对 MdRender 组件的简单封装。

## 目录

## API

### 参数 Props

``` ts
export type MdRenderProps = {
  children: object | string
  className?: string
  enableCopy?: boolean
}
```

| 属性 | 说明 | 类型 | 默认值 |
| --- | --- | --- | --- |
| `children` | JSON 字符串或对象 | `object \| string` | - |
| `className` | 自定义类名 | `string` | - |
| `enableCopy` | 是否开启复制代码功能 | `boolean` | `true` |

## 演示示例

### 展示 JSON 字符串

<Playground code={JsonRenderStringRaw} title='展示 JSON 字符串' description='展示 JSON 字符串的渲染结果。'>
  <JsonRenderString />
</Playground>

### 展示 JSON 对象

<Playground code={JsonRenderObjectRaw} title='展示 JSON 对象' description='展示 JSON 对象的渲染结果。'>
  <JsonRenderObject />
</Playground>

## 源码

``` tsx
import MdRender from './MdRender'

export type JsonRenderProps = {
  children: object | string
  className?: string
  enableCopy?: boolean
}

function JsonRender({ children, className, enableCopy = true }: JsonRenderProps) {
  let jsonData = ''
  if (typeof children === 'string') {
    jsonData = children
  } else {
    jsonData = JSON.stringify(children, null, 2)
  }
  return (
    <MdRender className={className} enableCopy={enableCopy}>{`\`\`\`json\n${jsonData}\n\`\`\``}</MdRender>
  )
}

export default JsonRender
```
